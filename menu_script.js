function navigateTo(page) {
    console.log("Navigating to:", page); // Debugging log
    window.location.href = `/${page}.html`; // Ensure correct navigation
}

function closeMenu() {
  // alert("Closing menu");
  // Add functionality to close the menu or navigate elsewhere
  
}
